name: Deploy Static Content to Pages

on:
  push:
    branches: ["main"] # Se il tuo branch principale si chiama master, cambia "main" in "master"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './site' # Specifica a GitHub di pubblicare solo la cartella con il sito pulito

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
